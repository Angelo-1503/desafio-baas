# Variáveis usadas pelo docker-compose.prod.yml (deploy em VPS).
# As variáveis específicas da API ficam em backend/.env.production.

DOMAIN_APP=app.seudominio.com
DOMAIN_API=api.seudominio.com
CERTBOT_EMAIL=voce@seudominio.com

DATABASE_PASSWORD=troque-por-uma-senha-forte
DATABASE_ROOT_PASSWORD=troque-por-outra-senha-forte
